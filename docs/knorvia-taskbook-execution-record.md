# Knorvia Studio 任务书执行记录

对应《Knorvia Studio 开发任务书与迭代路线》（T00–T13）。本文件记录真实基线、实际执行的命令与结果、未执行项与阻塞项；**不使用任务书的待办当作完成声明**。状态采用：待执行 → 进行中 → 待验收 → 已完成。

## T00 执行基线与最小变更边界

| 项目         | 实际值                                                                                                                                                           |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 仓库         | `https://github.com/accomplish07zrh-eng/knorvia-studio`（私有，具有 push 权限）                                                                                  |
| 工作目录     | `D:\tools\knorvia-studio`                                                                                                                                        |
| 分支         | `main`                                                                                                                                                           |
| 基线提交     | `bcc63b6ff684e8ee9b8070f326d70ac007ece19b`（与任务书规划基线完全一致，未回退分支）                                                                               |
| 版本         | `0.8.0-preview.3`（收尾时从 `0.8.0-preview.2` 升级：远端 `v0.8.0-preview.2` 已指向旧基线 `bcc63b6`，按同版本不可变规则不能再往该标签发布；原标签与附件保留不动） |
| 检出时工作区 | 干净（`git status --porcelain` 0 行）                                                                                                                            |
| 新鲜度检查   | `node scripts/check-workspace-freshness.mjs` 退出 0，`基线新鲜：main（与 origin/main 同步），ahead 0 / behind 0（阈值 50）`                                      |

工具链（含后续补齐的固定版本复验）：`mise.toml` 固定 `node=24.14.0`、`pnpm=10.33.2`；本机未安装 mise，早期检查在系统 Node `v26.3.0` 上用 `npx pnpm@10.33.2` 执行（pnpm 版本与锁定一致），Python `3.13.14`。**后续已下载官方 Node `v24.14.0` 并在该固定版本上重跑整套门禁：`install --frozen-lockfile` / `typecheck` / `lint` / `fmt:check` / `architecture:check` / `build:cli-packages` 全部退出 0，`test:studio` 751/751 通过 0 失败；交付的便携包也改用 Node 24.14.0 重新构建**，因此原先「未在固定 Node 版本上复验」的偏差已关闭。`node_modules/ssh2` 的可选 crypto 原生绑定在 Node 26 下编译失败（可选优化，安装脚本自行跳过，`pnpm install` 整体退出 0）；Node 24 下 `install --frozen-lockfile` 同样退出 0。

路径核对：任务书 R/C/U 简称与 T02–T09 点名的文件全部存在；`packages/cua` 存在但为 fail-closed 占位实现；`examples/plugins` 原仅有一个示例。基线检查（`pnpm typecheck` / `lint` / `fmt:check` / `architecture:check`）在基线上全部通过（`fmt:check` 首轮唯一失败项是本轮新增且尚未格式化的 7 个文件，格式化后 4458 文件全部合规，据此判定基线自身格式干净）。

## 批次进度

| 任务    | 状态                                  | 提交                                                                              | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------- | ------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T00     | 已完成                                | `7a23eb9` / 本文件                                                                | 基线、路径核对、现行 spec 与已有实现复核                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T01     | 主体已实现，收尾待外部条件            | `797a76e` + `94805dd`                                                             | 同提交发布门禁与版本不可变已实现；**dry run 已改为真正执行只读不可变校验**，发布判定测试已纳入统一入口并做过「故意破坏必须失败」验收，版本已升到 `0.8.0-preview.3`。**待补**：`main` 分支保护需仓库管理员配置（见 `docs/knorvia-release-admin-rules.md`，未声称已配置）；云端发布验收需一次真实 CI 运行                                                                                                                                                                                                          |
| T02     | 已完成                                | `657a00b`                                                                         | 测试稳定性与诊断                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T03     | 已增加有效保护，遗留项待补            | `d62c610` + `a3c9c7f`                                                             | 迁移前一致快照与便携数据保护已实现；交付脚本不再用字符串长度截取相对路径，Windows 长短名/空格/中文/末尾分隔符与篡改反例均有真实用例。**遗留**：Agent 会话库无迁移前备份、任务索引对未知更新账本按 `none` 放行（见风险清单 1–2）                                                                                                                                                                                                                                                                                  |
| T04     | 后端与界面已提交，真实 CLI 验收待执行 | `18acac7` + `a882ae9`                                                             | 分层探测状态、能力矩阵、界面动作与发送前校验                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T05     | 主体已实现，展示层待验收              | `b6fb13c` + `bafd088`                                                             | 交付摘要、多选审阅与接纳记录已实现；**群聊接纳记录已改为按业务运行身份写入与查询**（含刻意让业务身份≠工作区身份的回归用例与重开核验）。**待补**：打包应用上的完整接纳展示验收                                                                                                                                                                                                                                                                                                                                    |
| T06–T08 | 契约与生产/消费/媒体路径均已接通      | `e4208d3` + `7011f67` + `0797b30` + `f337368` + `2a939d2` + `5d988d2` + `f357ed4` | 输出契约、引用解析、参数绑定与四个模板已落地；**命名输出已由真实执行入口生产**（text/json/file 三种来源，文件由 Host 核对哈希），**跨隔离输入已受控交接**（上游文件与创作媒体两条），并跑通端到端「盘点 → 批准 → 整理 → 查看修改 → 接受文件」与「创作节点 → 读取真实媒体引用」。**已补**：文档路径已在打包应用的工作流界面上验收（`scripts/t13-workflow-acceptance.mjs`，**6/6 PASS**，含应用后真的关闭重开核验）；**待补**：创作媒体路径的界面验收（服务层已有真实 CreationService + 回环供应商 + 真 PNG 用例） |
| T09     | 服务与界面已提交，媒体交接已接通      | `3dc3a62` + `6ee777d` + `2a939d2` + `f357ed4`                                     | 参数快照、来源关系、只读远端核验、创作界面入口与 i18n；创作节点从真实 `job.id` 产出 `creation-output`（带 `fileName` 与真实哈希）并经创作服务核对证据；媒体成果由 Host 经 CreationService 解析后受控导入下游工作区。**待补**：真实创作协议验收（需授权）                                                                                                                                                                                                                                                         |
| T10     | 已完成（性能目标未全达）              | `81b2073` + `6e1b4f2`                                                             | 可复现性能基线、运行历史收敛与两库并行准备；冷启动仍约 5.5 秒，**未达 3 秒探索目标**                                                                                                                                                                                                                                                                                                                                                                                                                             |
| T11     | 包与面板已提交，实际安装验收待补      | `48894e8` + `11a5660`                                                             | 三个技能包、兼容矩阵与设置内只读兼容面板                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T12a    | 已完成                                | `dc51d26`                                                                         | 受限契约、失败关闭门禁与默认关闭实验开关                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T12b    | 未实现（后置）                        | —                                                                                 | 真实 Driver 与观察/操作链路：当前接线固定返回不可用，真实请求必然拒绝；**不得称为已交付可用的受限桌面控制**                                                                                                                                                                                                                                                                                                                                                                                                      |
| T13     | 待验收                                | `c8faf54` + `48ded35`                                                             | 批次验收与便携交付已完成（单聊全路径 + 群聊隔离执行 → 差异审阅 → 用户接纳 + 工作流界面「盘点 → 批准 → 整理 → 查看修改 → 接受文件」；`0.8.0-preview.3` 已构建交付且 `data` 逐字节未变）。**不能在 Windows CI 结果未确认、上述链路未全部闭合时判为已验收**                                                                                                                                                                                                                                                         |

## 批次门禁（从仓库根执行，全部为真实结果）

对完整批次（T01–T12）在干净工作区执行：

| 命令                      | 结果                                                                         |
| ------------------------- | ---------------------------------------------------------------------------- |
| `pnpm typecheck`          | 退出 0                                                                       |
| `pnpm lint`               | 退出 0，0 warnings / 0 errors                                                |
| `pnpm fmt:check`          | 退出 0，`All matched files use the correct format`                           |
| `pnpm architecture:check` | 退出 0，`violations: 0 / baseline: 0 / new: 0`（未修改基线、未抬高任何上限） |
| `pnpm build:cli-packages` | 退出 0                                                                       |
| `pnpm test:studio`        | 退出 0，**tests 751 / pass 751 / fail 0 / skipped 0**                        |

回归规模变化：基线 571 → 608（T02/T03/T06/T09）→ 643（T04/T05）→ 662（T07）→ 689（T08/T11）→ 713（T04 UI 与 T10 落地后）→ 728（T09 UI 落地后）→ 751（T11 UI 与 T12 落地后）。

过程中发现并修复的真实问题（均已单独提交）：

1. `d142571`：T06 有意把工作流文件信封升到版本 2 并继续接受版本 1，旧断言把版本 2 当作未知版本而失败；改为断言「当前版本 + 1」被拒绝，并新增版本 1 与当前版本可读的正向断言，保留原用意。
2. `b6fb13c` 内含修复：T05 的交付投影在缺少可选 `checkpoint` 的旧运行上抛错（`runOutcomeProjection.ts`），已改为可选链并补回归用例。
3. `7011f67` 内含修复：T07 报告指出 UI 参数送不到服务端，已由 Lead 补齐 `useWorkflowExecution.run(text, params)` 与 `WorkflowEditor` 的透传。

## 各任务要点与未执行项

### T01 同提交发布门禁与版本不可变（`797a76e`）

新增可复用质量工作流 `studio-quality.yml`（检查定义只保留一份并输出受检 SHA），离线检查与发布工作流都改为调用它；PR 上同时跑 Linux 与 Windows；发布改为 `needs` 阻断；移除 `--clobber`，用 `scripts/release-immutability.mjs` 判定同版本幂等/拒绝（11 个夹具用例全通过），保留既有 prerelease 判断。分支保护必须由管理员配置，已单列 `docs/knorvia-release-admin-rules.md`，**未声称已配置**。

### T02 测试稳定性（`657a00b`）

**实测结论：任务书所述「6 个 Windows 失败」在本机不可复现**（修复前三个文件各跑 3 次即全绿）。因此只修三个已确认的健壮性/诊断缺陷：python 解释器确定性解析（含 Store 占位判据，缺失时失败而非跳过）、创作 `terminal()` 超时给出 taskId/最后状态/事件序列并区分确定失败与结果未知、定时工作流改用注入时钟并删除固定 sleep（新增两个确定性用例）。`scripts/test-studio.mjs` 的子进程超时属条件项，未执行整轮验证前不引入，缺口已登记。

### T03 升级与便携数据保护（`d62c610`）

新增 `VACUUM INTO` 一致快照（WAL 活跃时绝不只复制主库）、迁移顺序改为 版本检查 → 备份 → WAL → 事务、备份失败即停止且原库零写入；11 例升级保护测试 + 3 例便携测试（含真实调用 `deliver-portable.ps1` 的端到端与反例）。遗留：Agent 会话库仍无迁移前备份；任务索引不识别更新的账本。

### T04 分层内核状态（`18acac7` + `a882ae9`）

四阶段（locate/version/protocol/auth）独立捕获，区分未安装与协议失败/超时/取消；修复 `externalUpdatePlan` 与 `isolation.close()` 抛错覆盖成功状态两个真实缺陷；版本化能力矩阵带证据级别，ACP 自报只能升级 resume/approval；认证默认 skipped 且不自动登录。界面在既有 Agent 界面显示逐阶段状态/机器码/说明/耗时，「已安装但协议失败」与「未安装」明确区分，缓存命中的协议段标注「来自缓存」；提供选择程序、重新探测、查看诊断、去原 CLI 登录（只读提示，不执行登录、不保存凭据、不联网）；发送前在业务入口做能力校验，拒绝时不发送任何内容，且不静默切换模型或扩大权限。Lead 补齐 UI 报告中的阻塞点：`StudioRuntimeService.inspectKernels` 之前丢弃参数，重新探测无法绕过缓存，已改为透传 `StudioKernelInspectOptions` 并加宽契约签名（向后兼容）。**未做**：无真实 CLI/模型调用；无 DOM 测试环境，组件渲染未运行时验证。

### T05 交付摘要与接纳（`b6fb13c`）

四类结论与证据优先级、只读投影接入既有载荷、多选批量应用沿用既有 `paths[]`、应用顺序 检查→加锁→应用→复核→单事务写接纳记录、重启三态展示；远端应用按设计永不记为已核验。68/68 通过。**未做**：无 DOM 测试环境，React 渲染未运行时验证。

### T06 输出契约（`e4208d3`）

`StudioOutputRef` 四类引用 + 版本语义（legacy/ok/unsupported/tooLarge）、有界内联与检查点边界、业务类型单一来源。**未做**：节点实际产出 `outputs` 与 `{{node.output.*}}` 取值留给 T07（T07 已完成引用解析侧）。

### T07 Host 引用与参数（`7011f67`）

纯引用判定与执行期解析、`importFile` 带来源记录复制、参数 schema 与冻结、受理时「节点要求 ∩ 用户授权」+ 真实能力核验后运行前拒绝、条件分支仍是受限语法。86/86 与 21/21 通过。**未做**：无本地写入方的 kernel-status 链路只完成一半；creation-output 引用执行期一律失败关闭。

### T08 工作流场景（`0797b30`）

四个场景（发布检查/代码审查/文档整理/内容与配图交付包）都声明少量参数、证据规则与失败行为；代码审查声明代码级只读权限，在不能强制只读的内核上按能力矩阵运行前拒绝（这是任务书要求的行为，不是缺陷）；文档整理走真实 approval 节点 + 既有差异审阅；配图不可用时标记未验证而不是占位。**未做**：无真实模型运行、无桌面视觉验收。

### T09 创作来源（`3dc3a62` + `6ee777d`）

参数快照与来源按白名单生成（凭据绝不入快照）、`reuseJob` 无副作用、`verifyJob` 只读查询且绝不重发（断言未发生第二次提交）、成果哈希。界面在既有创作历史卡片上提供四个入口（沿用参数 / 变体 / 作为参考继续生成 / 核验远端结果），门控复用服务端同一谓词 `creationReferenceSlots`，不可用入口给出可解释原因；快照与来源只按记录渲染，旧记录显示「无法还原参数」而不是用当前设置回填；核验只由用户显式触发，绝不自动重发。**未做**：真实协议未真机验证；无 DOM 测试环境；openai-images 无法远端核验。

### T11 技能包与兼容面板（`48894e8` + `11a5660`）

三个原创技能包（项目交接/资料整理/文档质量检查），每个含 manifest、`compatibility.json`、SKILL.md 固定 8 段契约、`skill-contract.json` 与五类夹具；兼容矩阵逐内核标注，运行时支持一律标未验证；许可为本次原创 MIT，刻意不复用带他人版权的示例文本。设置内的只读兼容面板已交付在既有插件管理入口内：严格解析边车（版本必须为 1、插件名必须与 manifest 一致），精确内核优先再回退通配，逐能力给出 available/unavailable/unverified 与机器可读原因，缺文件/畸形/不可读一律降级为 unknown；宿主只有在插件已启用且已枚举到技能时才声明 `skills.enabled-catalog`。Lead 修正了三处已与现实矛盾的文档，并把测试中「文档必须说明面板已推迟」的断言改为「必须说明面板只读且不得再声称界面不存在」。**未验证**：无 DOM 环境，面板渲染未验证；三个包未安装进应用；真实模型运行未做。

### T10 性能（`81b2073` + `6e1b4f2`）

先测量后优化，并如实记录测量条件：本机 Node `v26.3.0`（非 mise 固定的 24.14.0）。真实基线：滚动 6 样本 p50 6.8–10.0ms、p95 20.1–26.7ms、>33ms 帧 0–2、挂载行 20、锚点位移 0/0px；桌面冷启动到首个引导页 5904–6430ms，**未达到 3 秒目标**，且热启动无改善，阶段拆分显示 Host fork → 首次可交互约占 65%。按证据实施了两项优化：

1. **运行历史有界化**（`81b2073`）：真实组件 100 次运行 × 8 步，挂载 442/471ms → 235/249ms，刷新 236 → 132/142ms，挂载步骤行 800 → 160，DOM 节点 3613 → 726，且可完整展开还原 101/101 次运行。
2. **并行准备两个数据库**（`6e1b4f2`）：复核确认 2.8–3.0 秒的存储准备是 tasks-index 与 CLI 会话库**两段串行**（不同文件、不同 WAL、互不依赖），已改为并行并保持阶段通知顺序与失败优先级不变。同机同命令前后对比：首次可交互 median 5799 → 5554ms、Host fork → 首次可交互 3782 → 3516ms、`[database-startup]` 2906 → 2654ms（收益约 250–300ms）。**`<3 秒` 仍未达标**（约 5.5 秒）；剩余固定成本是 15.9MB CLI 包启动、SQLite 建库/WAL/fsync，以及约 0.8s Host 启动、0.24s 服务构造、0.18s 端口/IPC/渲染，全部仍在同一条串行链上；要达标必须把「首次可交互」与数据库就绪解耦，属产品/架构决策，未擅自改动。

明确未做：未删除第 2/3 个工作目录的会话库准备（相对 `sessionDbPath` 按各自 cwd 解析，会导致某个库静默未准备）；未跳过快照/backfill/账本校验；未在 `prepare` 完成前释放端口或挂载服务。未改动 `kernelRegistry.ts`（本机未安装 CLI 内核，无法复现探测开销）。**已披露未修**：`packages/rpc/src/logging-middleware.ts` 对每次 RPC 调用按 info 记录；main/渲染包体积未优化。

### T12 受限 Computer Use（`dc51d26`，受限期）

只读复核确认 `packages/cua` 是刻意 fail-closed 的占位包（无 `src/`，`createComputerUseRuntime().execute()` 固定返回不可用，`isReadOnlyBrokerMethod` 恒为 false，全仓库仅一个 CUA 相关隔离测试）。因此本轮交付的是**受限契约 + 失败关闭门禁 + 默认关闭开关**，而不是可用的桌面控制：`specs/knorvia-cua-restricted.md` 固定观察的身份/版本/有效期、允许动作与目标窗口、过期观察绝不重放、停止优先的固定顺序、未知结果绝不自动重放，以及审批与结果仍由既有 Studio Runtime Host 独占；`packages/cua/restricted-gate.js` 为纯函数门禁，在实验关闭/Driver 不可用/Helper 不可用/权限不足/观察过期或作用域不符/窗口不匹配时一律拒绝；既有失败关闭导出**未被放宽**，没有任何能力被改成可用；设置内的实验开关默认关闭、会话内有效、不启动 Helper、不触发系统权限弹窗、不联网。**未验证（如实）**：Windows 上没有可启动的 Helper/Driver，没有观察路径，没有真实点击/输入，没有真实模型决策，没有逐内核运行校验，停止与未知结果语义只有纯逻辑覆盖，界面渲染无 DOM 环境可验。

## 未解决风险汇总

1. Agent 会话库（`apps/cli/.../storage/session-store/migration-runner.ts`）仍无迁移前备份。
2. 任务索引遇到未知的更新账本会按 `none` 放行（静默降级解释），防护点在中心迁移逻辑，本轮未动。
3. ~~`scripts/test-studio.mjs` 仍无子进程超时，挂起的测试无界。~~ **已关闭**：runner 现在传入 `--test-timeout`（默认 120000ms，`KNORVIA_TEST_TIMEOUT_MS` 可覆盖），挂起用例以 `test timed out after <n>ms` 失败并非 0 退出；刻意不做整轮子进程强杀（Windows 杀进程树易误伤）。证据：一个永不 resolve 的用例在 `--test-timeout=2000` 下 2 秒内以该诊断失败，默认值下 `pnpm test:studio` 仍 751/751。
4. **云端 CI 事实更正**：先前记录的「云端 CI 从未运行」已不成立。据用户通过 GitHub 插件核对，`main` 上的运行 **`36173833394`** 中 `linux / quality` 通过、**`windows / quality` 失败**（Windows 的类型检查、Lint、格式、架构检查、CLI 构建均通过，`Offline Studio regression` 失败：751 项测试 750 通过 1 失败）。失败项是 `packages/desktop/test/portable-upgrade-preserves-data.test.ts` 的真实交付脚本用例，报 `scripts/deliver-portable.ps1` 的 `Delivered program file differs from build: ld\Knorvia Studio.exe`。
   - **本机已复现该失败条件并修复**：Windows CI 上 `%TEMP%` 常是 8.3 短名（`C:\Users\RUNNER~1\...`），`Resolve-Path` 保留短名而 `Get-ChildItem` 返回长名，旧实现用 `FullName.Substring($root.Length + 1)` 算相对路径因此算错（本机用短名复现得到 `e-probe-directory\build\Knorvia Studio.exe`，与 CI 的 `ld\...` 同一类）。已改为遍历时逐级拼接相对路径，不再做长度截取，并在失败诊断里输出双方完整路径与根。
   - **云端已转绿（据用户核对）**：`e82e9f3` 对应 **`Studio offline checks #76`，运行编号 `36224595039`，`success`**（2026-09-26 06:50:39 UTC）；**最新提交 `65191fe` 对应 `Studio offline checks #83`，运行编号 `36230025967`，`success`**，Linux 与 Windows 的质量检查、构建与离线回归均已通过。**该阻塞项关闭**，记录以 #83 为准（不是沿用 #76 的结果）。
   - **区分两件事**：通过的是 `Studio offline checks`，**不是**一次 Windows 发布工作流的完整 dry run。发布侧仍需一份固定 SHA 的「检查 → 打包 → `validate-release`」成功记录；正式发布再由明确的发布操作放行。**不能把普通 CI 转绿等同于发布路径已实测。**
   - 本机**读不到 GitHub Actions**：这是本执行环境的访问限制（未安装 `gh`）。仓库当前为**公开**（`private: false` / `visibility: public`，据用户核对），先前记录里「仓库私有」的说法已不成立并在此更正。运行结论均以用户核对为准，本机只声称复现了失败条件并修复。
5. 无任何真实模型、真实 CLI、真实 SSH、真实付费调用；全部为离线夹具与依赖注入替身。
6. ~~Node 24.14.0（mise 固定）未复验。~~ **已关闭**：已在官方 Node v24.14.0 上重跑整套门禁（751/751）并用它重建交付便携包。

## 回滚

- 按任务回退对应提交；代码回滚不自动撤销用户已应用的文件。
- **历史可构建性**：当前 HEAD 已恢复可构建；**历史中存在一个已知不可构建的提交 `5d988d2`**
  （它漏提交了 `runExecutor.ts` 的导入改动）。后续补齐（`dcb2aae`）**不会让旧提交变得可构建**。
  用 bisect 定位其他问题时不必一律从 `dcb2aae` 起算：遇到 `5d988d2` 且其构建失败与正在定位的问题无关时，
  可在 bisect 会话中跳过该提交（自动 bisect 用退出码 `125` 表示无法测试）；
  **若定位的恰好就是这次类型检查破坏，它应当算 bad，不能跳过。**
- 数据恢复：应用完全退出后，用 T03 生成的 `<db>.pre-<起点>.<时间戳>.bak` 覆盖回数据库并清除 `-wal`/`-shm`，步骤见 `docs/knorvia-upgrade-protection-report.md`。
- 便携包回退：用上一版已验证的 `win-unpacked` 重新执行 `scripts/deliver-portable.ps1`，脚本会保证 `data` 不被替换。
- 每个任务的独立回滚说明见各自的 `docs/knorvia-*-report.md`。
