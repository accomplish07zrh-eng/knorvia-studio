# 运行历史与修改检查：2026-09-23

保持现有运行历史与 Dialog 布局，不新增成果页面。范围仅运行历史组件及其局部异步状态。

- 后端继续拥有运行事实和已受理操作。UI 临时 owner 按 targetId + service 隔离，关闭订阅、切换目标或服务后使旧 UI 回执失效；已受理后台操作继续收口。
- 每个运行操作独立防同步重复提交，读取差异或应用文件不阻塞其他运行的停止按钮。停止受理后保持“正在停止”，与后端 cancelRequested 共同控制展示，不重复触发。
- 重试明确标为“重试未完成步骤”；未知结果仍用原确认弹窗。确认期间离开当前上下文后，旧确认结果不能再发出操作。
- 修改弹窗立即打开加载态，可在读取期间关闭。每次打开拥有独立代际；旧读取、旧应用回执及旧刷新不能重开弹窗或覆盖后打开的文件。
- 应用失败与差异加载失败在弹窗内部展示，保留重试读取与再次应用入口；应用已受理后关闭弹窗不会取消后台工作，也不再为已关闭弹窗读取差异。
- 前后内容具有清晰标签、无文件与空文件区别、现有 diff 语义配色；长路径可换行。字号继续使用原 UI tokens。

```
target + service → UI scope → per-operation admission → backend accepted work
                       └→ dialog generation → current-only result/error projection
scope closed / dialog closed ───────────────→ invalidate late UI receipts
```

离线回归覆盖同步重复点击、停止与文件操作并行、切目标／服务的旧确认、弹窗关闭后迟到读取／应用、弹窗重开与错误归属。不使用模型或用户数据。

已完成 7 项离线异步边界回归，修改范围 lint 0 警告／0 错误，架构检查通过。真实桌面视觉与交互由主工作区合并后的打包验收执行。
