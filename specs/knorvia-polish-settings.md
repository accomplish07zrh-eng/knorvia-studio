# 实际存储位置与引导完成状态

2026-09-23，补充主规格 `knorvia-polish-20260923.md`。保留原设置与引导布局。

- 设置服务是数据位置的唯一事实源，新增只读 `getDataLocation()` 返回运行时 `baseDir`、`dataRootDir`、`readOnlyReason`。路径来自现有路径解析器，不由 Renderer 拼接。固定原因是 `portable`、`environment` 或 `null`；便携优先。便携／显式环境固定目录的迁移请求在复制前被服务拒绝。
- 设置页从当前服务读取实际位置。加载、错误和重试明确显示，不回退到用户主目录。固定目录只读展示实际应用数据根与原因，不显示选目录／保存迁移按钮。非固定环境继续使用原迁移流程，不新增迁移机制。
- 非固定路径选择器的未提交目标由自身保留。进入保存、失败等状态不重置目标，目录选择失败显示错误，不误报复制失败。选择和保存使用同步互斥锁，异步结果仅更新当前组件；窄布局可收缩、状态可访问。
- 本机职业偏好保存成功即完成引导；附属记录失败或 5 秒超时不撤销完成，不在下次启动重弹。已有职业不发自动引导请求；手动设置入口仍能显示引导。关闭／Esc 仍仅取消当前次，不保存。
- 完成引导和服务／职业变化取消旧判定，旧 RPC 不得恢复引导。记录超时计时器在成功、失败和超时后清理。

```text
Host 路径解析器 → getDataLocation → 当前服务的设置卡片
保存偏好成功 → markOnboarded（取消旧判定）→ 关闭引导 → 附属记录写入（失败只记录）
```

离线验收覆盖便携与环境只读及实际路径、固定迁移不复制；引导已保存职业、旧响应取消、超时与记录失败。通过对应 lint 和服务类型检查后交主库最终 UI 验证。测试只使用独立临时目录，不触碰桌面 data 或实际 CLI。
