# Visual evidence

Use `nodeRepl.emitImage(await tab.screenshot())` in the same awaited cell. Do not stringify, print or return raw screenshot bytes. Inspect the image before making visual claims. DOM observations establish semantic targets; screenshots establish appearance. Never claim that a structure-only check proves layout fidelity.
