{
  "name": "knorvia-agent-workspace",
  "version": "0.16.9",
  "private": true,
  "description": "A cross-platform TypeScript Node.js CLI with a small TUI and optional SEA packaging.",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "build:sea": "pnpm --filter @knorvia/cli build:sea",
    "check": "pnpm registry:check && turbo run typecheck",
    "clean": "turbo run clean",
    "cli:build": "pnpm --filter @knorvia/cli build",
    "cli:dev": "pnpm --filter @knorvia/cli dev",
    "cli:upload-sea": "pnpm --filter @knorvia/cli upload:sea",
    "dev": "pnpm --filter @knorvia/cli dev",
    "format": "oxfmt --write package.json \"packages/**/*.json\" \"packages/**/*.ts\" \"packages/**/*.mjs\"",
    "format:check": "oxfmt --check package.json \"packages/**/*.json\" \"packages/**/*.ts\" \"packages/**/*.mjs\"",
    "lint": "pnpm --dir ../.. exec oxlint apps/cli",
    "lint:count": "node scripts/count-ts-lines.mjs",
    "lint:fix": "turbo run lint:fix",
    "prompt-trajectory:model-io": "pnpm --filter @knorvia/prompt-trajectory model-io --",
    "prompt-trajectory:prompt": "pnpm --filter @knorvia/prompt-trajectory record:prompt --",
    "registry:check": "node scripts/generate-bash-command-registry.mjs --check",
    "registry:generate": "node scripts/generate-bash-command-registry.mjs",
    "release": "node ../../node_modules/release-it/bin/release-it.js --config .release-it.json",
    "release:dry": "pnpm run release --release-version",
    "sea": "pnpm --filter @knorvia/cli sea",
    "typecheck": "pnpm -r --filter \"./packages/*\" typecheck"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "proxy-agent": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@withfig/autocomplete": "2.692.3",
    "c8": "^11.0.0",
    "esbuild": "^0.25.0",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "postject": "^1.0.0-alpha.6",
    "tsx": "^4.20.0",
    "turbo": "^2.4.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": "24.14.0"
  },
  "packageManager": "pnpm@10.33.2"
}
